<UserControl x:Class="SPGen2010.Components.Controls.Configures_UserDefinedFunction_Scale"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" MinHeight="25" MaxHeight="25" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Foreground="White" Background="#3f5478">Configures</Label>
        
        <StackPanel Grid.Row="1" Background="White" Name="_Configures_StackPanel">
            <StackPanel.Resources>
                <Style TargetType="Label">
                    <Setter Property="TextElement.FontFamily" Value="SimSun" />
                    <Setter Property="TextOptions.TextFormattingMode" Value="Display"></Setter>
                    <Setter Property="Cursor" Value="Hand"></Setter>
                    <Setter Property="Margin" Value="5,0,5,0"></Setter>
                    <Setter Property="Foreground" Value="Blue"></Setter>
                </Style>
            </StackPanel.Resources>
        </StackPanel>
    </Grid>
</UserControl>
